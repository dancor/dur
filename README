Install (run from this directory to install dur to existing ~/bin in $PATH):
~ cabal install -pO
~ ln -s $(pwd)/dur $(pwd)/mydate ~/bin

Usage: dur
- Sorts all filenames >= 100MB metric to ~/big-files-(date)-.txt

Todo: kill du process on ^C (haskell lamely leaves it running?)
